const fs = require("fs");

module.exports = async (fastify) => {
    // fastify.get("/*", async (request, reply) => {
    //     reply.type("text/html").send(fs.createReadStream("./public/index.html"));
    // });
};
